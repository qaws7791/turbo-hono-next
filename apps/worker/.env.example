# Worker app (BullMQ)
#
# 대부분의 값은 `apps/api/.env.example`와 동일하게 사용합니다.
# 로컬 개발에서는 기본으로 `apps/api/.env`를 읽도록 설정되어 있습니다.
# 필요 시 아래 변수로 어떤 워커를 실행할지 제어할 수 있습니다.
#
# 예) WORKERS=material,plan
# 예) WORKERS=material
WORKERS=material,plan
