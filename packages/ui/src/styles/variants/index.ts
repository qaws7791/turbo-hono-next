/**
 * Common style variants used across UI components
 * These are tailwind-variants configurations that are reused by multiple components
 */
export { buttonVariants } from "./button-variants";
export {
  inputGroupVariants,
  inputVariants,
  textAreaVariants,
} from "./input-variants";
