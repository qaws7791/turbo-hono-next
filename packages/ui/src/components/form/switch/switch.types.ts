import type { SwitchProps as AriaSwitchProps } from "react-aria-components";

/**
 * Props for the Switch component.
 * A toggle control that can be turned on or off.
 */
export type SwitchProps = AriaSwitchProps;
