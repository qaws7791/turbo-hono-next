/**
 * AI 채팅 관련 타입 정의
 * @repo/ai-types의 공통 타입을 재export
 */

// Re-export types from @repo/ai-types
export type {
  MessageRole,
  Message,
  Conversation,
  StoredToolInvocation as ToolInvocation,
} from "@repo/ai-types";
